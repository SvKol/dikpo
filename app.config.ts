export default defineAppConfig({
    ui: {
        horizontalNavigation: {
            base: 'group relative w-full flex items-center gap-2 px-3 py-4 rounded-md font-medium text-lg focus:outline-none focus-visible:outline-none dark:focus-visible:outline-none focus-visible:ring-inset focus-visible:ring-2 focus-visible:ring-primary-500 dark:focus-visible:ring-primary-400 disabled:cursor-not-allowed disabled:opacity-75',
            icon: {
                base: 'flex-shrink-0 w-8 h-8 relative'
            }
        }
    }
})